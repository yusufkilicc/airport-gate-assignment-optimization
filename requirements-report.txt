# Optional tooling — only needed to regenerate the Word report and the
# PowerPoint deck (src/build_report_docx.py, src/build_presentation.py,
# src/build_inde4902_template_report.py). Not required to run the dashboard.
-r requirements.txt
python-docx==1.1.2
python-pptx==1.0.2
